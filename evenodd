#include<stdio.h>
int main()
{
  long int n;
  scanf("%ld",&n);
  if(n>o && n<=100000)
  {
    if(n%2==0)
    {
      printf("even");
    }
    else
    {
      printf("odd");
    }
   else
   {
      printf("invalid");
    }
}
